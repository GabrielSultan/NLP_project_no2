# Core
pandas>=1.5.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.12.0
wordcloud>=1.8.0
gdown>=4.6.0
openpyxl>=3.0.0

# NLP
nltk>=3.8.0
simplemma>=1.0.0
snowballstemmer>=2.2.0
gensim>=4.3.0
scikit-learn>=1.2.0

# Hugging Face
transformers>=4.30.0
# torch: default pip wheel is often CPU-only; for NVIDIA GPU use the CUDA command from
# https://pytorch.org/get-started/locally/ then pip install the rest of this file.
torch>=2.0.0
tensorboard>=2.14.0

# Spell correction
pyspellchecker>=0.7.0

# Interpretation
shap>=0.42.0

# Application
streamlit>=1.25.0

# RAG / semantic search (Streamlit app + rag_pipeline.py)
sentence-transformers>=2.2.0
rank-bm25>=0.2.2

# Optional — not imported by app_streamlit or rag_pipeline (uncomment if you need it elsewhere)
# langchain>=0.1.0
